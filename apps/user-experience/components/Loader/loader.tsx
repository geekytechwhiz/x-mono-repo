import Box from "@mui/material/Box";
import LinearProgress from "@mui/material/LinearProgress";

export default function PlatformXLoader() {
  return (
    <Box sx={{ width: "60%" }}>
      <LinearProgress />
    </Box>
  );
}
