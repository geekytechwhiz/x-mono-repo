// import PageScroll from '../Common/PageScroll';
import PageScroll from "./PageScroll";

const PollPageScroll = ({ icons, parentToolTip, srollToView }) => {
  return <PageScroll icons={icons} parentToolTip={parentToolTip} srollToView={srollToView} />;
};
export default PollPageScroll;
