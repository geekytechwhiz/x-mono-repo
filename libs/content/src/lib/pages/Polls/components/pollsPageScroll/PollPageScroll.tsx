// import PageScroll from '../Common/PageScroll';
import PageScroll from "./PageScroll";

const PollPageScroll = ({ icons, parentToolTip, scrollToView }) => {
  return <PageScroll icons={icons} parentToolTip={parentToolTip} scrollToView={scrollToView} />;
};
export default PollPageScroll;
