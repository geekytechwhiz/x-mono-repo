import { DraftIcon, EditorIcon, PublisherIcon, ReviewerIcon } from "@platformx/utilities";

export const icons: { [index: string]: string } = {
  request_review: DraftIcon,
  review: EditorIcon,
  approval: ReviewerIcon,
  publish: PublisherIcon,
};
